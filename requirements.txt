# ============================================================
# Flood Risk Prediction System — India Edition
# Python 3.13 | RTX 4050 CUDA-accelerated
# ============================================================
#
# INSTALL ORDER:
# 1. Install GDAL first (see note below)
# 2. Install PyTorch with CUDA:
#      pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
# 3. Then install everything else:
#      pip install -r requirements.txt
#
# GDAL NOTE (Windows):
#   pip cannot build GDAL from source on Windows.
#   Install pre-built wheel from: https://github.com/cgohlke/geospatial-wheels/
#   Or use conda: conda install -c conda-forge gdal
# ============================================================

# --- Core Scientific Stack ---
numpy>=2.0
pandas>=2.2
scipy>=1.14
scikit-learn>=1.5

# --- Geospatial Processing ---
rasterio>=1.3
geopandas>=1.0
shapely>=2.0
fiona>=1.10
pyproj>=3.6
# GDAL — install separately (see note above)
# GDAL>=3.7
rioxarray>=0.17
xarray>=2024.6
whitebox>=2.3                  # WhiteboxTools for terrain analysis (TWI, flow accumulation)

# --- Satellite Data Access (No-GEE Path) ---
pystac-client>=0.8             # STAC API catalog search
stackstac>=0.5                 # STAC → xarray lazy loading
planetary-computer>=1.0        # Microsoft Planetary Computer data hub
odc-stac>=0.3                  # Open Data Cube STAC loader

# --- Deep Learning (PyTorch + CUDA for RTX 4050) ---
# Install separately with CUDA:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
# torch>=2.4
# torchvision>=0.19
# torchaudio>=2.4

# --- Gradient Boosting ---
xgboost>=2.1
lightgbm>=4.5                  # Alternative ensemble member
optuna>=4.0                    # Bayesian hyperparameter optimization

# --- Dask Distributed Computing ---
dask>=2024.8
distributed>=2024.8

# --- Data Validation ---
pydantic>=2.9
pydantic-settings>=2.4

# --- API Layer ---
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27                    # Async HTTP client
slowapi>=0.1.9                 # Rate limiting
python-multipart>=0.0.12
websockets>=13.0

# --- Dashboard ---
streamlit>=1.38
folium>=0.17                   # Leaflet maps
streamlit-folium>=0.22
pydeck>=0.9                    # Deck.gl 3D visualization
plotly>=5.24
altair>=5.4

# --- Visualization ---
matplotlib>=3.9
seaborn>=0.13

# --- Data Formats ---
h5py>=3.12
netCDF4>=1.7
zarr>=2.18                     # Cloud-optimized array storage
pyarrow>=17.0                  # Parquet I/O

# --- Copernicus / GloFAS ---
cdsapi>=0.7                    # CDS API client for EWDS downloads
cfgrib>=0.9                    # GRIB → xarray engine (GloFAS format)
eccodes>=2.38                  # ECMWF GRIB codec (cfgrib backend)

# --- Utilities ---
tqdm>=4.66
requests>=2.32
aiofiles>=24.1
python-dotenv>=1.0
click>=8.1                     # CLI interface
loguru>=0.7                    # Structured logging
pyyaml>=6.0
tenacity>=9.0                  # Retry logic for flaky APIs
joblib>=1.4

# --- Testing ---
pytest>=8.3
pytest-asyncio>=0.24
pytest-cov>=5.0

# --- Code Quality ---
ruff>=0.6
mypy>=1.11
